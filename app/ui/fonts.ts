import {Inter} from 'next/font/google'

export const inter = Inter({subsets: ['latin']});
export const font2 = Inter({subsets: ['vietnamese'], weight: "700"})
export const lusitana = Inter({subsets: ['vietnamese'], weight: "700"})